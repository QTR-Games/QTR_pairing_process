pairings extra ideas beyond base program

add in the concepts for the 'penalty for pairing at 5" whatever that means.

# Create labels and entry fields for the "To avoid:" section
    #avoid_frame = tk.Frame(root)
    #avoid_frame.pack(side=tk.TOP, fill=tk.X, padx=5, pady=5)

    #for _ in range(3):
        #row_frame = tk.Frame(avoid_frame)
        #row_frame.pack(side=tk.TOP, fill=tk.X, pady=2)
        #tk.Label(row_frame, text="To avoid:").pack(side=tk.TOP, anchor="w", padx=5)
        #tk.Entry(row_frame).pack(side=tk.TOP, anchor="w", padx=5)





• NEW REQUEST TO ADD SOME KIND OF METRIC TO NUDGE THE VALUE OF A MATCHUP BASED ON WHETHER OR NOT YOU ARE PICKING TABLES.

• BATTLEFIELD ADVANTAGE MODIFIER (BAM)

	First idea is to allow a universal 'tolerance' number. This will allow a quick and dirty nudge on the fly to make the app easier to use on the day of an event.

	To help support teams that plan more and have more depth, we could eventually expand the grid of input values to accept a 2nd and 3rd qualifier for each cell in the grid currently.

	So for Dan Vs. Matt, if the matchup is normally a 3:

	My "Battlefield Advantage" is a 1;
	This means if I get to pick the table, my rating against matt would go up a whole letter grade because the tables are just that good for me.
	because I would go UP a whole letter grade (from a 3 to a 4) if I get to pick the table.

	My Battlefield Disadvantage is a 0.5;
	If I am not able to pick the table, this only influences the matchup a little bit. I only go DOWN half a letter grade from a 3 to a 2.5

• ADD SUPPORT FOR CHANGING THE RATINGS AGAINST THE ENEMY TEAM BASED ON THE PRESENT SCENARIO

• for sum_leaf_values within the tree_generator class - What if we put a toggle in here to allow users to switch between different types of matchup ratings?
	It could be useful to use the original method of just summing all the things. 
	This could widen the gap and reduce the amount of nodes that a user must scroll through

• For tree_generator - Need to add an operation to SORT the nodes at every level of the tree so that it will display the optimized node at the top.
	I think it would be best to implement this as a button after the tree has been generated. 
	This will allow the user to toggle it back and forth as they need during pairings.


Instead of coloring the entire row, we want to highlight only the specific combinations.
a 2/5 is orange a 3/5 yell etc

Make the grid of matchups larger

inverting or sorting descending or ascending after the matchups are generated